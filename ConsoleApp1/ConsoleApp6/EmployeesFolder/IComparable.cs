namespace ConsoleApp6.EmployeesFolder
{
    interface IComparable
    {
        int CompareTo(object obj); // Comparar um objeto a outro,
                                   // 1 caso o primeiro nome seja maior, -1 caso seja menor e 0 caso seja igual (maior,menor em relação a ordem alfabetica).
    }
}
